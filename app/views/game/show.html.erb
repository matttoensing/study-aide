<h3>Which Format Would You Like to Start?</h3>

<%= button_to "Flashcards", "/flashcards/#{@quiz.id}", params: { type: :flashcards }, method: :get, class: 'btn btn-primary' %>

<%= button_to "Quiz", "/assessments", params: { quiz_id: @quiz.id, user_id: current_user.id }, method: :post, class: 'btn btn-success' %>
